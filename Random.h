#define _CRT_SECURE_NO_WARNINGS
#pragma once

#include <stdio.h>
#include <stdlib.h>
#include <time.h>

int generateRandomNumber(int lowestValue, int highestValue);//Get lowest and highest value, returns a generated random number between.
void printRandomNumber(int lowestValue, int highestValue);//Get lowest and highest value, prints a generated random number between.